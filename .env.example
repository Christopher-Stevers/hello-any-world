# This file is mounted into the container at /app/.env
# so your express_server (cwd=/app/express_server) loads it via ../.env

DATABASE_URL="postgres://user:pass@host:5432/dbname"
SOME_API_KEY="replace-me"